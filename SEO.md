# SEO — «Красивая Ты» (красивая-ты.рф)

Задачи по поисковой оптимизации. Статус на 2026-09-24.

Легенда: `[ ]` — не сделано, `[~]` — частично, `[x]` — готово.

---

## Что уже есть

- `[x]` `<title>` и `<meta name="description">` на обеих страницах, осмысленные и с ключами.
- `[x]` Open Graph + Twitter Card, `og:image` 1200×630.
- `[x]` `<html lang="ru">`, `<meta name="robots" content="index, follow">`, `theme-color`.
- `[x]` Семантическая вёрстка: `<header>/<main>/<section>/<footer>/<nav>`, `aria-label`, скип-ссылка.
- `[x]` H1 на главной (`Студия красоты Натальи Манохиной`).
- `[x]` Блок «Направления» виден (не скрыт) — 5+ карточек направлений со ссылками на якоря прайса.
- `[x]` HTTP→HTTPS через certbot (`--redirect`), `charset utf-8`, кэш-сниппет `static-cache.conf`.
- `[x]` `alt` проставлены у всех `<img>`.

---

## 1. Структурные данные (JSON-LD) — приоритет 1

Главное упущение для локального бизнеса. Добавить `<script type="application/ld+json">` в `<head>`.

- `[x]` **LocalBusiness / BeautySalon** на обеих страницах:
  - `name`: Студия красоты «Красивая Ты»
  - `address` (`PostalAddress`): Высоковольтная ул., 22, ЖК «Парк на Фабричной», Раменское, Московская обл., RU
  - `geo` (`GeoCoordinates`): lat `55.573735`, lon `38.202501` (из виджета Яндекс Карт)
  - `telephone`: +7 965 195-22-77
  - `openingHours`: `Mo-Su 10:00-20:00`
  - `priceRange`: например `₽₽`
  - `url`, `image` (og-image или фото студии)
  - `sameAs`: `https://t.me/krasivayati`, `https://vk.ru/id162018962`, `https://max.ru/join/-TuE7SKXJ31PdEtAcn-28jNBv14W3XG6q3lgyBe5lx4`
  - `aggregateRating`: `ratingValue` 5.0, `reviewCount` 234, `bestRating` 5 (источник — Яндекс Карты, org 29792745953; держать в синхроне с блоком «234 оценки»)
  - `review`: 4 текстовых отзыва из блока `.quotes` на главной (Полина Гудач, Мария Е., Наталия Б., Ксения Абрамова), только на `index.html`. Добавить сюда же, если на странице появятся новые текстовые отзывы.
- `[x]` **BreadcrumbList** на `price.html` (Главная → Цены).
- `[x]` **OfferCatalog / Service** — 8 направлений прайса размечены в `hasOfferCatalog` на главной, ссылки на якоря `price.html#cat-*`.
- `[x]` **FAQPage** — блок «Вопросы и ответы» на главной (`#faq`), 10 пар Question/Answer с микроразметкой (`itemscope`/`itemtype="https://schema.org/Question|Answer"`). Текст утверждён студией 2026-09-19.

Проверять через Rich Results Test и Яндекс Вебмастер → «Проверка разметки».

---

## 2. Технические файлы и мета — приоритет 1

- `[x]` `robots.txt` в корне (+ `Disallow` на `SEO.md`, `price.txt`, `contacts.txt`).
- `[x]` `sitemap.xml` (`/` и `/price.html`, `lastmod`).
- `[x]` `<link rel="canonical">` на обеих страницах — абсолютный URL страницы.
- `[x]` `apple-touch-icon` 180×180 + `site.webmanifest` (иконки 192/512) — файлы пересозданы из актуального SVG-фавикона 2026-09-24, `assets/icons/`.
- `[x]` Отдельная страница `404.html` в дизайне сайта, с `noindex, follow`, абсолютными ссылками на ресурсы и переходами на главную/прайс — добавлена 2026-09-24. На сервере её нужно назначить обработчиком HTTP 404 (`error_page 404 /404.html;`).
- `[x]` Яндекс.Метрика не подключается осознанно: сайт не использует веб-аналитику и не передаёт ей данные посетителей. Пустой ID не активирует загрузку скрипта Метрики.
- `[x]` Служебные файлы `price.txt` и `contacts.txt` (черновики, отдавались напрямую как есть) удалены из репозитория 2026-09-19.
- `[~]` Сам `SEO.md` остаётся в репозитории (нужен для трекинга) и при деплое по-прежнему физически доступен по прямой ссылке — `robots.txt` только просит не индексировать, не блокирует отдачу. Настоящий фикс — `deny-hidden.conf` / `location ~* \.md$ { deny all; }` в конфиге сайта на сервере (нужен доступ по SSH, отдельный шаг).

---

## 3. Заголовки

- `[x]` `price.html` — визуальный заголовок «Цены полностью» повышен до `<h1>` (класс `.h2` сохранён, вид не изменился).
- `[x]` H1 главной дополнен гео/услугой через `.sr-only`-текст (визуальный дизайн не менялся, утверждённый вид сохранён): «…— студия красоты в Раменском: перманентный макияж, косметология, ногтевой сервис» (2026-09-19).
- `[~]` Часть H2 разбита `<br>` + `<em>` и абстрактна («Три повода прийти сейчас») — на ранжирование влияет слабо, можно не трогать.

---

## 4. Контент / on-page

- `[~]` Сайт одностраничный (+ прайс) → слабо ранжируется по разным услугам. Направления сейчас — карточки-якоря на прайс. Варианты роста:
  - минимум: под каждым направлением дать 1–2 абзаца описательного текста с локальными ключами прямо на странице;
  - максимум: отдельные страницы услуг (`/permanentnyj-makijazh`, `/kosmetologiya`, …) с уникальным текстом, ценами, фото, разметкой `Service`.
- `[~]` `alt` фотографий студии дополнены гео-контекстом («студия „Красивая Ты“, Раменское»). Портфолио оставлено как есть — там описания по процедурам, «Раменское» в каждый alt = переспам.
- `[x]` Гео-контекст: добавлен вопрос «Как добраться до студии?» в `#faq` (адрес, ЖК «Парк на Фабричной», ссылка на раздел «Контакты») — 2026-09-19. Остальные площадки под гео-текст (блок «О студии», вступление «Направлений») пока не трогали — не хотим дублировать одно и то же по всей странице (см. ниже).
- `[x]` Внутренняя перелинковка: карточки направлений ведут на соответствующие якоря прайса, а категории прайса — на релевантные разделы главной. Сделано 2026-09-19.

---

## 5. Скорость (Core Web Vitals)

- `[x]` Тяжёлые изображения пережаты (2026-09-03), `assets` 14 МБ → 2.3 МБ:
  - `hero.png` 2.5 МБ → `hero.webp` 234 КБ (это LCP; ссылки в `styles.css` и preload обновлены);
  - `og-image.png` 677 КБ → `og-image.jpg` 98 КБ (мета-теги и JSON-LD обновлены);
  - удалено 20 неиспользуемых файлов на 7.5 МБ (в т.ч. `natalia.jpg` 4 МБ, `2/5/6/7/8/9.jpg`, `12/21/22/26/27/29/31/32.jpg`, `salon-02/04/05/06.webp`);
  - остальные JPEG/WebP пережаты в 1920px / mozjpeg q80.
- `[x]` Все реально используемые изображения (hero, faq-фон, мобильный hero, портфолио, фото студии, Наталья, diag/statia, отзывы — миниатюры и полный размер) переведены в `.webp` (sharp, q75–82, с уменьшением до размера показа). 2026-09-19: `assets` по этим файлам 4.68 МБ → 1.7 МБ (−64%), самый тяжёлый файл `hero-mobile-flower.png` 1.3 МБ → 20 КБ. `<picture>`/avif не делали — прямая замена `.jpg/.png` → `.webp` даёт тот же эффект без лишней разметки, современные браузеры webp поддерживают все.
- `[x]` Шрифты уже оптимальны: `fonts.css` использует вариативные файлы (`font-weight: 400 700` в одном `.woff2`) с разбивкой `unicode-range` на кириллицу/латиницу — не 4 отдельных начертания на семейство, как предполагалось. 6 файлов, 12–28 КБ каждый (~108 КБ суммарно), `font-display: swap`. Marck Script используется дважды для мелких декоративных подписей — вес незначительный, трогать не нужно.
- `[x]` CLS: всем `<img>` без `width`/`height` (портфолио, фото студии) атрибуты проставлены 2026-09-19.
- `[ ]` Прогнать PageSpeed Insights / Lighthouse и зафиксировать реальные цифры (Core Web Vitals) — не делал, нужен реальный аудит на задеплоенной странице. Ориентир по весу страницы на 2026-09-19: изображения на главной ~1.7 МБ (было 4.68 МБ), preload hero разбит по `media` на десктоп/мобильный вариант, карта грузится лениво через `IntersectionObserver` (см. `index.html`).

---

## 6. Вне сайта (не код, влияет сильнее всего)

- `[ ]` Яндекс Бизнес, 2ГИС, Google Business Profile — карточки с NAP (название/адрес/телефон) один-в-один с сайтом.
- `[ ]` Яндекс Вебмастер: добавить сайт, залить sitemap, задать регион «Раменское», подтвердить.
- `[ ]` Google Search Console: добавить, залить sitemap.
- `[ ]` Ссылки на сайт из профилей VK / Telegram / Max / 2ГИС / Яндекс Карт.
- `[ ]` Следить, чтобы рейтинг и число отзывов на сайте совпадали с Яндекс Картами.

---

## 7. Домен

- `[x]` **Свой домен подключён 2026-09-03: `красивая-ты.рф`** (`xn----7sbah3ak6bjn4gvb.xn--p1ai`).
  - Отдельный конфиг `/etc/nginx/sites-available/krasivaya-ty.conf` + свой сертификат Let's Encrypt (`красивая-ты.рф` и `www`), тот же webroot `/home/albi/static_sites/krasivaya`.
  - `www` → 301 на без-www.
  - `krasivaya.asama.site` → 301 на `красивая-ты.рф` (блок в `asama_site_extra.conf`, 443 отдаёт `return 301`).
  - canonical / og:url / og:image / JSON-LD `@id`/`url`/`image` / хлебные крошки / `sitemap.xml` / `robots.txt` — все на punycode-домен.
  - `[ ]` Осталось: сменить NAP-ссылки в карточках-листингах (Яндекс Бизнес, 2ГИС) на новый домен; в Вебмастере указать переезд (Яндекс: «Переезд сайта»).

---

## Ближайший пакет работ по коду

1. ~~JSON-LD: `LocalBusiness` (обе страницы) + `BreadcrumbList` (`price.html`).~~ Сделано 2026-09-03.
2. ~~`robots.txt` + `sitemap.xml`.~~ Сделано 2026-09-03.
3. ~~`<link rel="canonical">` на обе страницы.~~ Сделано 2026-09-03.
4. ~~`<h1>` на `price.html`.~~ Сделано 2026-09-03.
5. ~~Пережатие `hero.png`, `og-image` и остальных тяжёлых картинок + чистка неиспользуемых.~~ Сделано 2026-09-03.

Отдельными этапами: страницы услуг, смена домена, внешние листинги.

---

## Следующий пакет

Сделано 2026-09-03:
- `[x]` `apple-touch-icon` 180×180 + `site.webmanifest` + иконки 192/512 (`assets/icons/`); актуальные PNG пересозданы 2026-09-24.
- `[x]` `Person` (`founder` Наталья Манохина) в разметке `BeautySalon` на обеих страницах — E-E-A-T.
- `[~]` `alt` фото студии дополнены гео-контекстом (портфолио не трогал — переспам).

Итог пакета:
1. ~~**`FAQPage`** — блок вопрос-ответ + разметка.~~ Сделано: блок на главной (`#faq`), 10 пар Question/Answer с микроразметкой, текст утверждён студией 2026-09-19.
2. **Описательный текст под направлениями** на главной — сделано 2026-09-19 для всех 7 направлений, ведущих в прайс (карточка «08 Диагностика кожи» не трогали — она и так ведёт на полноценный раздел `#diagnostika` этой же страницы). Реализовано прямо в карточках `#napravleniya`: `.proc__card` переведены из `<a>` в `<details>` — по клику карточка сама разворачивается вниз, показывая абзац текста и ссылку «Смотреть цены», без отдельного дублирующего списка ниже (первая версия с отдельным блоком `.dirs__more` под каруселью была заменена — на мобильном она визуально дублировала те же названия сразу под карточками).
   Технически: `.proc__track` — всегда `align-items: flex-start` (карточки не растягиваются друг под друга); одинаковая высота закрытых карточек держится через `min-height` (`.proc__card` 245px, `.proc__summary` 176px на `min-width: 621px`), а не через выравнивание по соседям — поэтому раскрытие одной ни на пиксель не двигает остальные семь. Кнопка «Смотреть цены»/«Подробнее» внутри `summary` — `position: absolute; bottom: 0` (не `margin-top: auto`, из-за вложенного `flex-grow` он неточно считал высоту и вылезал за карточку на десктопе).
   Тексты утверждены студией 2026-09-19, заменены финальными версиями (черновики не использовались). Заодно под тот же паттерн `<details>` переведена и 8-я карточка «Диагностика кожи» — текст утверждён, ссылка «Узнать больше» по-прежнему ведёт на раздел `#diagnostika` этой же страницы.
3. ~~**Шрифты** — урезать веса.~~ Проверено 2026-09-19: не нужно, уже вариативные `.woff2` с разбивкой по `unicode-range`, суммарно ~108 КБ.
4. ~~**Микроразметка отзывов**~~ Сделано 2026-09-19: 4 текстовых отзыва блока `.quotes` размечены как `Review` в JSON-LD `BeautySalon` (рядом с `aggregateRating`).
5. ~~**Внутренняя перелинковка** прайс ↔ разделы главной.~~ Сделано 2026-09-19: во всех 7 категориях `price.html` в `.cat__about` добавлена ссылка на релевантный раздел главной (портфолио, диагностика кожи или FAQ). Обратная связь (главная → прайс) уже была у карточек направлений.
6. ~~**Галерея через `<picture>`** webp/avif + fallback.~~ Сделано иначе 2026-09-19: прямая замена на `.webp` без `<picture>` (эффект тот же, разметка проще).
7. На сервере: заблокировать отдачу `\.md$` (`SEO.md` по-прежнему доступен напрямую, `price.txt`/`contacts.txt` уже удалены из репозитория 2026-09-19).
8. **Яндекс Вебмастер + Google Search Console** — добавить `красивая-ты.рф`, залить sitemap, регион «Раменское», в Яндексе оформить «Переезд сайта» с `krasivaya.asama.site`.
