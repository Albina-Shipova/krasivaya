# SEO — «Красивая Ты» (krasivaya.asama.site)

Задачи по поисковой оптимизации. Статус на 2026-08-30.

Легенда: `[ ]` — не сделано, `[~]` — частично, `[x]` — готово.

---

## Что уже есть

- `[x]` `<title>` и `<meta name="description">` на обеих страницах, осмысленные и с ключами.
- `[x]` Open Graph + Twitter Card, `og:image` 1200×630.
- `[x]` `<html lang="ru">`, `<meta name="robots" content="index, follow">`, `theme-color`.
- `[x]` Семантическая вёрстка: `<header>/<main>/<section>/<footer>/<nav>`, `aria-label`, скип-ссылка.
- `[x]` H1 на главной (`Студия красоты Натальи Манохиной`).
- `[x]` Блок «Направления» виден (не скрыт) — 5+ карточек направлений со ссылками на якоря прайса.
- `[x]` HTTP→HTTPS через certbot (`--redirect`), `charset utf-8`, кэш-сниппет `static-cache.conf`.
- `[x]` `alt` проставлены у всех `<img>`.

---

## 1. Структурные данные (JSON-LD) — приоритет 1

Главное упущение для локального бизнеса. Добавить `<script type="application/ld+json">` в `<head>`.

- `[ ]` **LocalBusiness / BeautySalon** на обеих страницах:
  - `name`: Студия красоты «Красивая Ты»
  - `address` (`PostalAddress`): Высоковольтная ул., 22, ЖК «Парк на Фабричной», Раменское, Московская обл., RU
  - `geo` (`GeoCoordinates`): lat `55.573735`, lon `38.202501` (из виджета Яндекс Карт)
  - `telephone`: +7 965 195-22-77
  - `openingHours`: `Mo-Su 10:00-20:00`
  - `priceRange`: например `₽₽`
  - `url`, `image` (og-image или фото студии)
  - `sameAs`: `https://t.me/krasivayati`, `https://vk.ru/id162018962`, `https://max.ru/join/-TuE7SKXJ31PdEtAcn-28jNBv14W3XG6q3lgyBe5lx4`
  - `aggregateRating`: `ratingValue` 5.0, `reviewCount` 234, `bestRating` 5 (источник — Яндекс Карты, org 29792745953; держать в синхроне с блоком «234 оценки»)
- `[ ]` **BreadcrumbList** на `price.html` (Главная → Цены).
- `[ ]` **OfferCatalog / Service** по крупным направлениям: перманентный макияж, косметология лица, коррекция фигуры, лазерная эпиляция, маникюр и педикюр. Полный прайс (153 позиции) размечать не обязательно — достаточно категорий.
- `[ ]` **FAQPage** — если добавим блок вопрос-ответ (кандидат: секция «9 из 10 женщин ошибаются в уходе»).

Проверять через Rich Results Test и Яндекс Вебмастер → «Проверка разметки».

---

## 2. Технические файлы и мета — приоритет 1

- `[ ]` `robots.txt` в корне (`User-agent: *`, `Allow: /`, `Sitemap: https://krasivaya.asama.site/sitemap.xml`).
- `[ ]` `sitemap.xml` (`/` и `/price.html`, `lastmod`).
- `[ ]` `<link rel="canonical">` на обеих страницах (сейчас нет) — абсолютный URL страницы.
- `[ ]` `apple-touch-icon` (180×180) и `site.webmanifest`; сейчас только один PNG-favicon.
- `[ ]` После деплоя проверить, что `SEO.md` и прочие `.md` не отдаются наружу (расширить `deny-hidden.conf` — заблокировать `\.md$`).

---

## 3. Заголовки

- `[ ]` `price.html` — **нет `<h1>`**. Визуальный H2 «Цены полностью» повысить до H1 или добавить H1 «Цены студии „Красивая Ты“ в Раменском».
- `[~]` H1 главной не содержит гео/услугу. Рассмотреть подзаголовок или уточнение: «…в Раменском · перманент, косметология, ногтевой сервис».
- `[~]` Часть H2 разбита `<br>` + `<em>` и абстрактна («Три повода прийти сейчас») — на ранжирование влияет слабо, можно не трогать.

---

## 4. Контент / on-page

- `[~]` Сайт одностраничный (+ прайс) → слабо ранжируется по разным услугам. Направления сейчас — карточки-якоря на прайс. Варианты роста:
  - минимум: под каждым направлением дать 1–2 абзаца описательного текста с локальными ключами прямо на странице;
  - максимум: отдельные страницы услуг (`/permanentnyj-makijazh`, `/kosmetologiya`, …) с уникальным текстом, ценами, фото, разметкой `Service`.
- `[ ]` `alt` у портфолио/галереи сделать описательными (процедура + «Раменское»), а не общими.
- `[ ]` Добавить на страницу больше текста с гео-контекстом: район, ЖК, ориентиры, соседние населённые пункты.
- `[ ]` Внутренняя перелинковка: с карточек направлений — на соответствующие якоря/страницы, обратно с прайса — на разделы.

---

## 5. Скорость (Core Web Vitals)

- `[ ]` Тяжёлые изображения — прогнать `scripts/optimize_images.js`:
  - `assets/images/hero.png` — **2.5 МБ** → webp/jpg ~150–250 КБ (это LCP, preload уже есть);
  - `assets/images/natalia.jpg` — **3.9 МБ**;
  - `assets/images/7.jpg` — **1.2 МБ**;
  - ещё ~15 файлов 150–280 КБ.
- `[ ]` Отдавать `<picture>` с webp/avif + fallback.
- `[ ]` Шрифты: 3 семейства (Oswald, Manrope, Marck Script) × 4 веса — урезать до реально используемых начертаний.
- `[ ]` Проверить CLS: у всех `<img>` заданы `width`/`height` (или `aspect-ratio`).
- `[ ]` Прогнать PageSpeed Insights / Lighthouse, зафиксировать базовые цифры здесь.

---

## 6. Вне сайта (не код, влияет сильнее всего)

- `[ ]` Яндекс Бизнес, 2ГИС, Google Business Profile — карточки с NAP (название/адрес/телефон) один-в-один с сайтом.
- `[ ]` Яндекс Вебмастер: добавить сайт, залить sitemap, задать регион «Раменское», подтвердить.
- `[ ]` Google Search Console: добавить, залить sitemap.
- `[ ]` Ссылки на сайт из профилей VK / Telegram / Max / 2ГИС / Яндекс Карт.
- `[ ]` Следить, чтобы рейтинг и число отзывов на сайте совпадали с Яндекс Картами.

---

## 7. Домен

- `[ ]` Сайт на `krasivaya.asama.site` — поддомен портфолио-домена. Потолок по позициям низкий.
  Для продвижения нужен свой домен (`krasivaya-ty.ru` и т.п.) + 301-редирект со старого адреса,
  обновление canonical/OG/sitemap/robots, перенос карточек-листингов на новый URL.

---

## Ближайший пакет работ по коду

1. JSON-LD: `LocalBusiness` (обе страницы) + `BreadcrumbList` (`price.html`).
2. `robots.txt` + `sitemap.xml`.
3. `<link rel="canonical">` на обе страницы.
4. `<h1>` на `price.html`.
5. Пережатие `hero.png`, `natalia.jpg`, `7.jpg` и остальных тяжёлых картинок.

Отдельными этапами: страницы услуг, смена домена, внешние листинги.
